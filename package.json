{
	"name": "minx-chatroom",
	"version": "2.0.0",
	"description": "A cozy chatroom powered by Cloudflare Workers + Durable Objects — public room first, private rooms on the side",
	"type": "module",
	"main": "src/index.js",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"check": "node scripts/check-frontend.mjs",
		"test:digests": "node scripts/test-digests.mjs"
	},
	"devDependencies": {
		"wrangler": "^4.118.0"
	}
}
